Todo (Alpha version):

- Remove the choice of template and layout, the layout it should be fixed (for copyrighters, and changeable for admin), so for example when I create a new content it have already the specify layout assigned, with the blocks default block assigned.
- The only editable fields it should be the one you see on screen and the options, such number of item per page
- each content it should be editable with <class="rain_editable content_id_{$content.content_id}">


- When you click on Add Block, the popup show the block type you can add, when you select one you should have different options, for example if you add a News module it should just load the News, instead if you add a Content module you should be able to add your content.
- Edit mode accessible to all users to create different revisions (like wiki)
- Static Content editable: images and content on the theme should be editable as well
- Menu editable, menu should be editable from the front end (check branch menu_sort)
- File module to see the list of files uploaded (masonry)
- Drag and drop to uploads image in the text (uncomment RainEdit upload script)
- Add sitemap on toolbar, site map should drag and drop nodes
- Installer module (improve the module)
- Change Aloha with Etch
- Content_type module to add new content type
- Auto upgrade. Manage the versions of components and core libraries
- Production mode, any fatal error it should bring to 404
- User list should be improved
- Search, should save the user search to create the autocomplete
- Automatic Google translate, Plugin
- Block filter: block type where you can create your query, example, last 10 content, of content_type news, template content_list.

- prepare documentation of Rain:
-- how to create a website and manage the contents
-- how to create a theme
-- how to create a module and how to add a content_type


Todo (next version)

- Convert to psr-0
- Prepare the package for composer
